# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
env:
  PACKAGE_OUTPUT_DIRECTORY: ${{ github.workspace }}\output
  NUGET_SOURCE_URL: 'https://nuget.pkg.github.com/chitulescui/index.json'
jobs:
  build:

    runs-on: self-hosted

    steps:
      # - uses: action/checkout@v3
      # - name: Setup .Net Core
      #   uses: actions/setup-dotnet@v3
      #   with: 
      #     dotnet-version: 8.0.x
          
      - name: Restore
        run: dotnet restore ./DesktopApp/APP/PizzaPlace/PizzaPlace.csproj
      - name: Build
        run: dotnet build ./DesktopApp/APP/PizzaPlace/PizzaPlace.csproj
      - name: Pack
        run: dotnet pack ./DesktopApp/APP/PizzaPlace/PizzaPlace.csproj --output ${{ env.PACKAGE_OUTPUT_DIRECTORY }}
      - name: Publish
        run: dotnet publish ./DesktopApp/APP/PizzaPlace/PizzaPlace.csproj --configuration Release --output ./publish
      - name: push  
        run: dotnet nuget push ${{ env.PACKAGE_OUTPUT_DIRECTORY }}\*.nupkg  --api-key ${{secrets.pipelineconnect}} -s ${{ NUGET_SOURCE_URL }}




    
    # - name: setup-msbuild
    #   uses: microsoft/setup-msbuild@v2
    # - name: .Net Build
    #   uses: EasyDesk/action-dotnet-build@v1.0.0
    #   with:
    #     build-args: PizzaPlace.csproj
    # - name: Test
    #   working-directory: chitulescui\PizzaPlace-Azure-DevOps-Pipelines\DesktopApp\APP\PizzaPlace
    #   run: dotnet test --no-build --verbosity normal
      
      
          
